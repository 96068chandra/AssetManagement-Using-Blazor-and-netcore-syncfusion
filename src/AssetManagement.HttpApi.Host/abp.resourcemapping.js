// Use const instead of var or let for variables that won't be reassigned
// Use meaningful names for variables
const componentExports = {
  // Use an array literal for an empty array, not an empty object
  aliases: [],
  // Use an array literal for an empty array, not an empty object
  clean: [],
  // Use an object literal for an empty object, not an empty object
  mappings: {},
};

// Export the object using module.exports
module.exports = componentExports;
